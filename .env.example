# Optional: override the analytics endpoint at build time.
# Otherwise edit the ENDPOINT constant in src/lib/analytics.js after deploying
# the Worker (see worker/README.md).
VITE_ANALYTICS_URL=https://morganfung-analytics.YOUR-SUBDOMAIN.workers.dev
